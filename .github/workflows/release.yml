name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (如 1.2.3，会自动加 v 前缀)'
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Resolve version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="v${{ github.event.inputs.version }}"
          else
            VERSION="${GITHUB_REF_NAME}"
          fi
          echo "tag=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Read VERSION file
        id: verfile
        run: echo "ver=$(cat VERSION)" >> "$GITHUB_OUTPUT"

      - name: Prepare build vars
        id: buildvars
        env:
          VERSION: ${{ steps.version.outputs.tag }}
        run: |
          COMMIT=$(git rev-parse HEAD)
          BUILDTIME=$(date -u '+%Y-%m-%dT%H:%M:%S')
          BRANCH=$(git rev-parse --abbrev-ref HEAD)
          LDFLAGS="-s -w -X github.com/cicbyte/siyuan-cli/cmd/version.Version=${VERSION} -X github.com/cicbyte/siyuan-cli/cmd/version.GitCommit=${COMMIT} -X github.com/cicbyte/siyuan-cli/cmd/version.BuildTime=${BUILDTIME} -X github.com/cicbyte/siyuan-cli/cmd/version.GitBranch=${BRANCH}"
          echo "ldflags=${LDFLAGS}" >> "$GITHUB_OUTPUT"

      - name: Build Windows
        env:
          GOOS: windows
          GOARCH: amd64
        run: go build -ldflags "${{ steps.buildvars.outputs.ldflags }}" -o dist/siyuan-cli_windows_amd64.exe

      - name: Build Linux
        env:
          GOOS: linux
          GOARCH: amd64
        run: go build -ldflags "${{ steps.buildvars.outputs.ldflags }}" -o dist/siyuan-cli_linux_amd64

      - name: Build macOS
        env:
          GOOS: darwin
          GOARCH: amd64
        run: go build -ldflags "${{ steps.buildvars.outputs.ldflags }}" -o dist/siyuan-cli_darwin_amd64

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          files: dist/*
          generate_release_notes: true
